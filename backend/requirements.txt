# Web 框架与运行时
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# 数据校验与配置
pydantic==2.10.4
pydantic-settings==2.7.0

# 数据库（PostgreSQL 16）
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# AI 编排与 LLM（DeepSeek / 智谱均走 OpenAI 兼容 SDK）
langgraph==0.2.60
openai==1.58.1

# 向量数据库（Milvus）——2.5+ 提供 AsyncMilvusClient（2.4 仅有同步 MilvusClient）
pymilvus==2.5.4
# pymilvus 依赖 pkg_resources；setuptools>=81 已移除该包，故固定 <81
setuptools==80.9.0
# pymilvus 的 environs 依赖 marshmallow <4（4.x 移除 __version_info__）
marshmallow==3.26.1

# 文档解析（厂商解析 / 文件服务）
pypdf==5.1.0
python-pptx==1.0.2
python-docx==1.1.2

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 固定 <4.1：passlib 1.7.4 与 bcrypt>=4.1 的 wrap-bug 检测不兼容（安装时勿升级）
bcrypt==4.0.1

# HTTP / 工具
httpx==0.28.1

# 测试与质量
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
mypy==1.14.0
