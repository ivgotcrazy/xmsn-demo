{
  "name": "xmsn-frontend",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:user": "pnpm --filter user-web dev",
    "dev:admin": "pnpm --filter admin-web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "gen:api": "pnpm --filter @xmsn/api generate"
  }
}
